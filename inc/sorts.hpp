#ifndef SORTS_HPP
#define SORTS_HPP

#include<vector>

namespace Sorts
{
    void Swap(int* a, int* b);
    void SelectionSort(std::vector<int>& vec);
    void BubbleSort(std::vector<int>& vec);
}

#endif